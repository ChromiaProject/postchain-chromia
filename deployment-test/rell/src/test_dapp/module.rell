module;

import lib.icmf.*;

entity city { key name; }

operation add_city (name) {
    create city (name);
    send_message("L_city", name.to_gtv());
}

query get_cities () { return city @* {} ( .name ); }

operation do_nothing (dummy: integer) {}
