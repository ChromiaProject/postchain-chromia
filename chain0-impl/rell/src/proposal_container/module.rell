module;

import ^.proposal.*;
import ^.model.*;
import ^.container.*;
import .proposal_container_limits.*;

@extend(require_container_available_for_removal) function(container) {
    require(empty(pending_container_limits @* { container }), "Container %s has pending proposals and can't be deleted. Resolve proposals first".format(container.name));
}
