@test module;

import o: ^.ops;
import ^^^.model.*;

val p1key = rell.test.keypairs.alice;

function p1() = provider @ { p1key.pub };

function setup_module() {
    rell.test.tx().op(o.initialize_test_module(p1key.pub)).run();
}
